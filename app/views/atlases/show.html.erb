<%= link_to "View all place tags in this atlas", atlas_tags_path(@atlas) %><%= " | "%><%= link_to "View all events in this atlas", atlas_events_path(@atlas) %><%= " | "%><%= link_to "Create new report", new_atlas_report_path(@atlas) %><%=" | "%><%= link_to "Case Manager", atlas_cases_path(@atlas) %><%= " | "%><%= link_to "Atlas Dashboard", atlas_path(@atlas) %><%= " | "%><%= link_to "User Home", root_path %>
<h2><%= @atlas.name? ? @atlas.name : "Untitled Atlas" %></h2>
<h3>Current Reports</h3>
<table>
	<thead>
		<tr>
			<th>Event</th>
			<th>Location</th>
			<th>Received At</th>
		</tr>
	</thead>
	<tbody>
		<% @reports.each do |r| %>
			<tr>
				<td><%= link_to r.event_name, atlas_event_path(@atlas.id, r.event_id) %></td>
				<td><%= link_to r.tag.title, atlas_tag_path(@atlas.id, r.tag) %></td>
				<td><%= r.format_date %></td>
			</tr>
		<% end %>
	</tbody>

</table>
<br>
