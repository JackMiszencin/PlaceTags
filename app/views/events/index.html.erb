<h2>All Current Events</h2>

<table>
	<thead>
		<tr>
			<th>Event</th>
			<th>Name</th>
			<th>Location</th>
		</tr>
	</thead>
	<tbody>
		<% @events.each do |e| %>
		<tr><td><strong><%= e.name %><strong></td></tr>
		<%- @reports = e.reports %>
			<% @reports.each do |r| %>
				<tr>
					<td><em>Report:</em></td>
					<td><%= r.event_name%></td>
					<td><%= r.location %></td>
				</tr>
			<% end %>
		<% end %>
	</tbody>
</table>